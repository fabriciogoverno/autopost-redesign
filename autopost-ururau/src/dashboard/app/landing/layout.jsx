import '../globals.css';

export const metadata = {
  title: 'AutoPost Ururau — Transforme notícias em posts para redes sociais',
  description: 'Sistema autônomo de geração e publicação automática de posts em Instagram, Facebook, WhatsApp, Twitter, LinkedIn e TikTok. Zero custo mensal.',
};

export default function LandingLayout({ children }) {
  return <>{children}</>;
}
